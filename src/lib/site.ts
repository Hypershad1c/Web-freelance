// Single source of truth for the production domain. Update this ONE
// value before deploying — sitemap.ts, robots.ts, canonical URLs, and
// JSON-LD all read from here instead of each hardcoding their own.
export const SITE_URL = "https://domify.ma";
export const SITE_NAME = "Domify";
export const SITE_PHONE = "+212600000000"; // placeholder — same number as WhatsAppButton, update together
